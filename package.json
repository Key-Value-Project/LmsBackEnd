{
	"name": "backend",
	"version": "1.0.0",
	"description": "keyvalue-training",
	"main": "dist/app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"info": "echo keyvalue training project using node + express",
		"start": "node dist/app.js",
		"dev": "nodemon app.js",
		"build": "rm -rf dist/ && npx tsc",
		"stage": "npm run build && npm run start"
	},
	"author": "shaheen",
	"license": "ISC",
	"dependencies": {
		"eslint": "^9.6.0",
		"express": "^4.19.2"
	},
	"devDependencies": {
		"@types/express": "^4.17.21",
		"@types/node": "^20.14.9",
		"typescript": "^5.5.3"
	}
}
